name: Workflow de Despliegue Continuo (CI/CD) a AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  integration:
    name: Integración Continua (Continuous Integration)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Lint Code & Checks
        run: echo "Verificando código del repositorio..."

  build-and-push-ecr-image:
    name: Construcción y Carga de Imagen Docker a AWS ECR
    needs: integration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, Tag, and Push Docker Image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY_NAME }}
          IMAGE_TAG: latest
        run: |
          REPO="${ECR_REPOSITORY:-usvisa-app}"
          docker build -t $ECR_REGISTRY/$REPO:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$REPO:$IMAGE_TAG

  continuous-deployment:
    name: Despliegue Continuo a Instancia AWS EC2
    needs: build-and-push-ecr-image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to AWS EC2 via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_PUBLIC_IP }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            REPO_NAME="${{ secrets.ECR_REPOSITORY_NAME }}"
            REPO_NAME="${REPO_NAME:-usvisa-app}"
            ECR_URI="${{ secrets.AWS_ECR_LOGIN_URI }}"
            ECR_URI="${ECR_URI%%/*}"
            REGION="${{ secrets.AWS_REGION }}"
            REGION="${REGION:-us-east-2}"
            
            sudo aws ecr get-login-password --region $REGION | sudo docker login --username AWS --password-stdin $ECR_URI
            sudo docker pull $ECR_URI/$REPO_NAME:latest
            sudo docker stop usvisa-container || true
            sudo docker rm usvisa-container || true
            sudo docker run -d -p 8080:8080 --restart unless-stopped --name usvisa-container \
              -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" \
              -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" \
              -e AWS_REGION="$REGION" \
              $ECR_URI/$REPO_NAME:latest
            sudo docker image prune -f
